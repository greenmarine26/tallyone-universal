# 작업 지침 — 이 폴더에서 일하는 모든 클로드

## ⭐ 버전을 말하기 전에 — 기억으로 답하지 않는다

이 저장소의 **검수앱·콘앱·벌크탤리는 버전 번호가 항상 같다.** 단일 진실원은 한 곳이다.

| 대상 | 버전 소스 (직접 읽는다) |
|---|---|
| 검수앱·콘앱·벌크탤리 | `src/utils.js` → `export const APP_VERSION` |
| 수집기 (별도 저장소 아님, PC 로컬) | `C:\TALLYTEST\TallymanMailCollector_*/app_gui.py` → `VERSION` |

- `bash build.sh`가 `sw.js`·`cone.html`(라벨/`__APPV`/`__CONEV`)·`bulk_tally.html`·`README.md`를 자동 동기화한다.
- ⛔ 콘앱 독자 버전 `C7.xx`는 V8.98-05에서 **폐기**됐다. 현재 버전으로 말하지 않는다.
- ⛔ **폴더명·ZIP 파일명은 버전의 근거가 아니다.** 리네임 누락으로 실제 13판 어긋난 적이 있다.
- 상세는 통합지침서 최상단 **"버전 정본표"**.

## 문서 규칙 (2026-07-26 사용자 확정 — 비협상)

- 통합지침서는 **최신 1개만** 존재한다. 새 판을 커밋할 때 옛 판을 같은 커밋에서 지운다.
  (안 지켜서 저장소에 48개, 드라이브에 28개가 쌓였다. 이제 `build.sh`가 자동으로 지운다.)
- `_인계_*.md` · `HANDOFF_*.md` · `변경_*.md`를 저장소에 남기지 않는다. 내용은 지침서에 흡수한다.
- 파일명이 `#Uxxxx`로 깨진 파일을 보면 즉시 지운다. 정상 파일과 중복된 **낡은 사본**이다.

## 배포

배포: 저장소 루트 `DEPLOY.md`와 드라이브 `CLAUDE_SHARED\★배포_표준절차_모든클로드_필독.md`를 따른다.
배포는 클로드가 끝까지 직접 한다 — 사용자에게 명령어를 주고 끝내지 않는다.
"이제 git push 하시면 됩니다" 같은 마무리 떠넘기기는 금지다.

작업 규칙·이력: 저장소 루트의 `평택항_검수_통합지침서_3앱통합본_V*.md`(최신 버전)를 시작 전에 읽는다.

저장소: `greenmarine26/greenmarinetally` (검수앱 + 콘앱 + 벌크탤리가 한 저장소)
