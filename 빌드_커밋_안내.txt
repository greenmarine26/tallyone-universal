Tallyman 검수앱 — DJCT 카고플랜 수정 / V8.22
=============================================
[적용된 변경]
 - DJCT처럼 코드≠선박명인 배에서, 매트릭스 수정이 카고플랜·베이플랜에 반영되도록 수정.
   (화면 조회를 빌더와 동일한 '코드'로 user 매트릭스를 먼저 찾게 통일)
 - 수정 파일: src/shipStructure.js, src/components/PrintableCargoPlanV2.jsx,
              src/components/BayPlan.jsx
 - 버전: src/utils.js APP_VERSION = V8.22

[★ 반드시 빌드 후 커밋 — src만 올리면 화면 안 바뀜]
 이 폴더에서:
   bash build.sh          ← assets/dist/sw.js/cone.html 라벨까지 V8.22로 빌드·동기화
   git add -A
   git commit -m "M8.22 DJCT 카고플랜 매트릭스 반영(vslCode 조회)"
   git push

 (이 zip에는 빌드 산출물(assets/dist)이 옛 V8.20-01 그대로입니다.
  build.sh가 새로 빌드하므로 반드시 먼저 실행하세요. node_modules는 자동 설치됩니다.)

[검증 완료]
 - esbuild 전체 컴파일 PASS, 실매칭코드 시뮬 PASS(구=계열대체→남의배, 신=user 매트릭스 구제).
