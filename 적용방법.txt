검수앱 DJCT 카고플랜 수정 — V8.20-02
====================================
[원인] 매트릭스 빌더는 '코드'로 저장/조회, 카고플랜·BayPlan(화면)은 '선박명'으로
       조회 → DJCT처럼 코드≠선박명인 배는 화면이 user 매트릭스를 못 찾고
       같은 계열 다른 배(XTPG 등) 구조를 빌려와, 매트릭스를 고쳐도 화면이 안 바뀜.

[수정] 화면 조회도 빌더와 같은 '코드(vslCode)'로 user 매트릭스를 먼저 찾도록 통일.
       - src/shipStructure.js        : getShipBayDictData에 vslCode 구제 분기
       - src/components/PrintableCargoPlanV2.jsx : voyageInfo→코드 추출해 조회에 전달
       - src/components/BayPlan.jsx   : 동일(3개 조회 + _vslCode)
       - src/utils.js                 : APP_VERSION V8.20-02

[검증] esbuild 전체 컴파일 PASS, 실매칭코드 시뮬 PASS(구=계열대체, 신=user 구제).

[적용]
 1) 이 4개 파일을 레포의 같은 경로에 덮어쓴다.
 2) bash build.sh   (assets/dist/sw.js 빌드+동기화 — 반드시 실행)
 3) git add -A && git commit -m "M8.20.02 DJCT 카고플랜 매트릭스 반영(vslCode 조회)" && git push
